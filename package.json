{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "A Telegram bot with machine learning capabilities",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "telegraf": "^4.0.0",
    "@tensorflow/tfjs": "^3.0.0",
    "axios": "^0.21.1",
    "dotenv": "^10.0.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}